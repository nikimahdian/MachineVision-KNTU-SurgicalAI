# configs/

YAML configs for training and simulation (for example `segmentation.yaml`, SOFA/env settings). Point training and eval scripts at these files; adjust paths and hyperparameters here rather than hard coding them in Python.
